func void hola()
{
    int x;
    x = 3;
}

inicio
{
    posicion(0,0);
    trazo(verdadero, 1); 
    color("negro");
    
    int contador;
    contador = x;
    int grados = x;

    mientras (contador < 13)
    {
        mover(20);
        girarIzquierda();
        mover(20);
        girarIzquierda();
        mover(20);
        girarIzquierda();
        mover(20);
        girarIzquierda();
        contador = contador + 1;
        rotar(grados);
    }

	x = 2; 
}
